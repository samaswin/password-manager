<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "SecureVault - Password Manager" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="application-name" content="SecureVault">
    <meta name="mobile-web-app-capable" content="yes">
    <meta name="theme-color" content="#ffffff">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%# User authentication and profile meta tags for Vue components %>
    <% if user_signed_in? %>
      <meta name="user-authenticated" content="true">
      <meta name="user-email" content="<%= current_user.email %>">
      <meta name="user-name" content="<%= current_user.email.split('@').first.titleize %>">
      <meta name="user-role" content="<%= current_user.role %>">
      <meta name="user-theme" content="<%= current_user.preferences&.dig('theme') || 'light' %>">
    <% else %>
      <meta name="user-authenticated" content="false">
    <% end %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%= vite_client_tag %>
    <%= vite_javascript_tag 'application' %>

  </head>

  <body data-theme="<%= user_signed_in? && current_user.preferences&.dig('theme') ? current_user.preferences['theme'] : 'light' %>">
    <div id="vue-app">
      <%# Vue Navbar Component %>
      <Navbar></Navbar>

      <%# Flash Messages %>
      <% if notice || alert %>
        <div class="container" style="margin-top: 20px;">
          <% if notice %>
            <div class="card-panel teal lighten-4 teal-text text-darken-4">
              <i class="material-icons left">check_circle</i>
              <%= notice %>
            </div>
          <% end %>
          <% if alert %>
            <div class="card-panel red lighten-4 red-text text-darken-4">
              <i class="material-icons left">error</i>
              <%= alert %>
            </div>
          <% end %>
        </div>
      <% end %>

      <%# Main Content %>
      <main>
        <%= yield %>
      </main>

      <%# Footer %>
      <footer class="page-footer" style="background-color: var(--navbar-bg); padding-top: 20px;">
        <div class="container">
          <div class="row">
            <div class="col l6 s12">
              <h5 class="text-gradient">SecureVault</h5>
              <p style="color: var(--text-secondary);">
                Secure password management with AES-256 encryption
              </p>
            </div>
            <div class="col l4 offset-l2 s12">
              <h6 style="color: var(--text-primary);">Links</h6>
              <ul>
                <li><a href="/about" style="color: var(--text-secondary);">About</a></li>
                <li><a href="/privacy" style="color: var(--text-secondary);">Privacy Policy</a></li>
                <li><a href="/terms" style="color: var(--text-secondary);">Terms of Service</a></li>
              </ul>
            </div>
          </div>
        </div>
        <div class="footer-copyright" style="background-color: var(--bg-tertiary);">
          <div class="container" style="color: var(--text-secondary);">
            © <%= Time.current.year %> SecureVault. Built with Rails & Vue.
          </div>
        </div>
      </footer>
    </div>
  </body>
</html>
